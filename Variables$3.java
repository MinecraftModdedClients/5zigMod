import com.google.common.base.Predicate;

class Variables$3
  implements Predicate<rr>
{
  Variables$3(Variables this$0) {}
  
  public boolean apply(rr entity)
  {
    return (entity != null) && (entity.ap());
  }
}
